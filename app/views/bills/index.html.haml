%h1 Listing my debts

- @bills_gap_debt_hash.each do |debtee, bills|
  .row
    .span2
      %b= debtee.name
    .span2.offset2
      = "Total: " << bills.map{|b| b.fee }.reduce(:+).to_s
      = link_to 'Clear', clear_bills_path(:debtee_id=>debtee.id), :class=>"btn btn-link", :method=> :post, :id=>"debt_#{debtee.id}"
  .row
    .span6
      %table{:class=>"table table-bordered"}
        %thead
          %tr
            %th{:width=>"30%"} Fee
            %th{:width=>"30%"} Restaurant
            %th Date
        - bills.each do |bill|
          %tbody
            %tr
              %td= bill.fee
              %td= bill.account.restaurant
              %td= l bill.created_at
